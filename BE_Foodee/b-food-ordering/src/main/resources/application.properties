spring.application.name=b-food-ordering

# --- DataSource (MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/foodee?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=vinh
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# --- JPA/Hibernate ---
# DB ?� import s?n -> kh�ng cho Hibernate t? s?a c?u tr�c
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# --- Server ---
server.port=8080

# --- JWT (gi? ?�ng key theo code c?a b?n: jwt.*) ---
# -> ??i secret th?t d�i/ng?u nhi�n, ??c t? bi?n m�i tr??ng
jwt.secret=${JWT_SECRET}
# Th?i gian s?ng token (ms). 86400000 = 1 ng�y
jwt.expiration=86400000

# --- Groq API key: ??c t? bi?n m�i tr??ng, kh�ng hardcode ---
groq.api.key=${GROQ_API_KEY}

# --- CORS cho FE Vite (c?ng 5173) ---
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# --- Thymeleaf (n?u kh�ng d�ng template) ---
spring.thymeleaf.check-template-location=false

# --- Logging ---
# Khi c?n debug security th� m?i b?t DEBUG
logging.level.org.springframework.security=INFO
